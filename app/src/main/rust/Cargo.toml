# Cargo.toml

[package]
name = "droidvnc-ng"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]
name = "droidvnc_ng"

[dependencies]
rustvncserver-android = { version = "1.3", features = ["turbojpeg"] }

[features]
default = []
debug-logging = ["rustvncserver-android/debug-logging"]
